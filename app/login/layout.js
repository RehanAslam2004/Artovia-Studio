export const metadata = {
    title: 'Sign In',
    description: 'Sign in to your Artovia Studio account to access your orders, downloads, and loyalty points.',
    robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
    return children;
}
