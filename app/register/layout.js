export const metadata = {
    title: 'Create an Account',
    description: 'Create your Artovia Studio account to start shopping, track orders, and earn loyalty points on every purchase.',
    robots: { index: false, follow: false },
};

export default function RegisterLayout({ children }) {
    return children;
}
